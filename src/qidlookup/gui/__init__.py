"""Optional Tkinter desktop GUI for qidlookup.

This package is a presentation layer only, exactly like ``qidlookup.cli``:
it talks to ``qidlookup.core`` and ``qidlookup.database`` and contains no
business logic of its own. Tkinter is part of the Python standard library
on Windows, so no extra dependency is required.
"""
